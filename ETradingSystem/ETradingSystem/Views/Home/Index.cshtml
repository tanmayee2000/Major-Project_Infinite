<!DOCTYPE html>
<html>
<head>
    <title>Ekart - Online Shopping Site</title>
    <link rel="stylesheet" type="text/css" href="~/Content/styles.css">
    <style>
        /* Add this CSS to make the video full screen and responsive */
        .video-container {
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100vh;
            overflow: hidden;
        }

            .video-container video {
                position: absolute;
                top: 0;
                left: 0;
                width: 100%;
                height: 100%;
                object-fit: cover;
            }

        /* Add this CSS to make the content appear on top of the video */
        .content {
            position: relative;
            z-index: 1;
            padding: 20px;
        }

        /* Add this CSS to style the search bar */
        .search-bar {
            display: flex;
            align-items: center;
            padding: 10px;
            border: 1px solid #ccc;
            border-radius: 5px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
            position: relative; /* Add this to make the search bar a positioning context */
        }

            .search-bar input[type="text"] {
                width: 600px;
                padding: 10px;
                border: none;
                border-radius: 5px;
                font-size: 16px;
            }

            .search-bar button[type="submit"] {
                padding: 10px 20px;
                background-color: #007bff;
                color: #fff;
                border: none;
                border-radius: 5px;
                cursor: pointer;
            }

                .search-bar button[type="submit"]:hover {
                    background-color: #0056b3;
                }

        /* Add this CSS to style the bucket list button */
        .bucketlist-button {
            padding: 10px 20px;
            background-color: #007bff;
            color: #fff;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            position: absolute; /* Add this to position the button absolutely */
            right: 20px; /* Add this to position the button 20px from the right edge */
            top: 10px; /* Add this to position the button 10px from the top edge */
        }

            .bucketlist-button:hover {
                background-color: #0056b3;
            }
    </style>
</head>
<body>
    <div class="video-container">
        <video autoplay loop muted>
            <source src="~/Images/welcome.mp4" type="video/mp4">
        </video>
    </div>
    <div class="content">
        <div class="container">
            <header>
                <div class="search-bar">
                    @using (Html.BeginForm("Search", "Home", FormMethod.Post))
                    {
                        <input type="text" name="searchTerm" placeholder="Search for product">
                        <button type="submit">Search</button>
                    }
                    <button class="bucketlist-button" onclick="location.href='@Url.Action("BucketList", "Home")'">Bucket List</button>
                </div>
                <p>Welcome to Ekart, your one-stop online shopping destination. Explore our vast collection of products and start shopping today!</p>
            </header>

            <section class="products">
            </section>

            <section class="products">
            </section>

        </div>
    </div>
</body>
</html>